export interface Tool {
  title: string;
  description: string;
  imageUrl: string;
  filter1: string;
  url: string;
  Tags?: string;
  rank?: number;
  // Add any additional fields that your API returns
} 